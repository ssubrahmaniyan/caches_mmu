package LLCache;

  (*synthesize*)
  module mkLLCache(Empty);
    rule rl_hello_world;
      $display("Hellow world\n");
      $finish();
    endrule: rl_hello_world
  endmodule: mkLLCache

endpackage
